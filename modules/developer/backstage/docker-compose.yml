# NOTE: Backstage requires a custom build - the cookiecutter image is just a template generator
# To use Backstage, you need to:
# 1. Run: npx @backstage/create-app
# 2. Build your custom Docker image
# 3. Update this file with your custom image
#
# For now, this service is disabled.

# Placeholder service to keep valid docker-compose
services: {}

networks:
  infra-network:
    external: true
