# PostgreSQL Catalog - connects to postgres-infra
connector.name=postgresql
connection-url=jdbc:postgresql://postgres-infra:5432/postgres
connection-user=${ENV:POSTGRES_USER}
connection-password=${ENV:POSTGRES_PASSWORD}
