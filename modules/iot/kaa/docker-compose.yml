# NOTE: KaaIoT is a commercial platform with no single public Docker image.
# It uses a microservices architecture deployed via Kubernetes/Helm.
# To use Kaa, you need:
# 1. A KaaIoT Cloud account or Helm charts from kaaiot.com
# 2. Configure your cluster following https://docs.kaaiot.io
#
# For now, this service is disabled.

# Placeholder service to keep valid docker-compose
services: {}

networks:
  infra-network:
    external: true
